[Unit]
Description=Renew ACME.sh managed certificates
After=network-online.target nss-lookup.target

[Service]
Type=oneshot
SyslogIdentifier=acme.sh
ExecStart="{{ (acmesh_home, 'acme.sh') | ansible.builtin.path_join }} --cron --home \"{{ acmesh_home }}\" --config-home \"{{ acmesh_home }}\"
